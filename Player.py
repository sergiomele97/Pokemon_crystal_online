# Clase Jugador: Modeliza los jugadores

class Player:
    def __init__(self, player_id, x_coord, y_coord):
        self.player_id = player_id
        self.x_coord = x_coord
        self.y_coord = y_coord
